# Change Log — Florin Neagu Notes

> One entry per publish, structural change, or significant update to the live site.
> Log after deploying. Reference ART/PRJ/SI IDs for tracked backlog items.
> When this file exceeds 50 data rows, archive the oldest rows to `_blog/_archive/change-log-YYYY.md`.

| Date | What changed | Why | Ref |
|---|---|---|---|
| 2026-07-14 | Brought the article template (`C:\GitFolder\00_templates\3_template_blog\docs\articles\_template-article.md`) to parity with the project template — added an optional "At a Glance" info box (setup guides/cheatsheets), a screenshot/image-conventions note (storage path, file naming, caption style — matching the project template's convention), an optional "Scope & Limitations" section for deep-dive articles, and a closing-paragraph + "← Back to [cluster]" nav-link convention (new for articles; no published article uses it retroactively). Extended the "Before publishing" checklist to match. Closes SI-003's remaining gap | Article template lacked the project template's other enhancements, flagged as open work in SI-003's notes | SI-003 |
| 2026-07-14 | Published "Blog Visibility & SEO for a MkDocs Site" (ART-004, `docs/articles/mkdocs-material/mkdocs-seo-and-visibility.md`) — practitioner checklist distilling this session's SI-008 through SI-013 visibility/SEO work (robots.txt, sitemap verification, meta descriptions, RSS, social cards, analytics, Search Console/Bing) into a reusable how-to, generalized to any MkDocs site rather than referencing this one's specific URLs; documents both Windows-only gotchas hit along the way (cairosvg's native Cairo dependency, the RSS plugin's path-separator bug) as callout admonitions. Nav entry added to MkDocs & Material (newest-first), Articles hub card and cluster index page updated, new "Visibility & SEO" subsection added to the cluster index | ART-004 publish | ART-004 |
| 2026-07-14 | Enabled social cards (Material `social` plugin), closing SI-010 — unblocked `cairosvg`'s native Cairo dependency on Windows via MSYS2 (installed MSYS2, ran `pacman -S mingw-w64-ucrt-x86_64-cairo` in the UCRT64 shell, added `C:\msys64\ucrt64\bin` to PATH) rather than the GTK3 runtime route D-028 had pointed to; `python -c "import cairosvg"` succeeded, `social` re-added to `mkdocs.yml`. Re-confirmed the RSS path-separator bug from D-028 is unrelated to Cairo and still reproduces (`feed_rss_created.xml` still emits `social/projects\index.png`-style mixed paths with `use_material_social_cards: true`), so that setting stays `false` — RSS readers get the theme default image, direct page shares get the real per-page card. `mkdocs build --strict` clean, 125 per-page card PNGs generated; verified live via `curl` (`og:image` meta tag present, image resolves) and a real link-preview screenshot supplied by the site owner. Committed and deployed via `mkdocs gh-deploy` | Completes SI-010, deferred earlier this session for lack of a Windows Cairo build | SI-010, D-028 |
| 2026-07-14 | Deleted `docs/manuals/github-in-a-nutshell/INDEX.md` and `docs/manuals/prisma-access-sase/INDEX.md` — leftover authoring artifacts (vault/PDF→chapter source-mapping tables used while compiling each manual, not reader-facing content) that were never in `nav` but still built and landed in `sitemap.xml` as duplicate content alongside the real overview pages. Confirmed zero incoming links anywhere in `docs/` before deleting. `mkdocs build --strict` clean, the "pages exist but not in nav" warning is gone, sitemap dropped 127 → 125 URLs | Removes crawlable duplicate content flagged during the Item 2 sitemap verification pass; closes the open issue in `_blog/issue-log.md` | — |
| 2026-07-14 | Added Bing Webmaster Tools verification file — site owner dropped `BingSiteAuth.xml` in `temp/`, copied to `docs/BingSiteAuth.xml` so it publishes at the site root. `mkdocs build --strict` clean, `diff` confirmed the built file is byte-identical to the source. Site owner still needs to click Verify in Bing Webmaster Tools once this deploys, then submit `https://somelamecode.github.io/mkdocs-blog/sitemap.xml` | Second search engine to index the site; Bing also feeds other search surfaces | SI-013 |
| 2026-07-14 | Google Search Console verified and `sitemap.xml` submitted (127 URLs) by site owner, following up on the SI-012 verification file deploy | Completes the GSC half of Item 8 | SI-012 |
| 2026-07-14 | Added Google Search Console HTML verification file — site owner started the GSC property setup for `https://somelamecode.github.io/mkdocs-blog/` and dropped the verification file (`google7edd60ce4d78d84d.html`) in `temp/`; copied to `docs/google7edd60ce4d78d84d.html` so it publishes unmodified at the site root. `mkdocs build --strict` clean, content matches Google's expected format exactly. Site owner still needs to click Verify in GSC once this deploys, then submit `sitemap.xml`. Bing Webmaster Tools (via GSC import or separate verification) still pending | Enables faster indexing than waiting on organic crawling; highest return-on-effort item in the visibility plan | SI-012 |
| 2026-07-14 | Wired up GoatCounter analytics (execution plan Item 7) — site owner signed up for GoatCounter (site code `somelamecode`, cookieless, no consent banner needed) over Google Analytics/Plausible/Fathom. Material's `extra.analytics` block only supports Google Analytics natively, so added `docs/javascripts/goatcounter.js` (creates the `<script data-goatcounter="...">` tag GoatCounter requires) and referenced it via `extra_javascript` in `mkdocs.yml` — loads on every page. `mkdocs build --strict` clean; confirmed the script tag renders correctly on both the homepage and a nested page (relative path resolves right in both cases) | First analytics on the site — needed before measuring whether any of the other visibility work (Items 1/2/3/5/6) actually moves traffic | SI-011 |
| 2026-07-14 | Confirmed with site owner: postpone social cards (SI-010) rather than install the GTK3 runtime to unblock `cairosvg` on Windows right now. No config change — `social` plugin stays out of `mkdocs.yml`, status stays deferred. Revisit via D-028's unblock steps whenever it's wanted | Explicit decision, not just an open blocker | SI-010 |
| 2026-07-14 | Meta description audit (execution plan Item 3) — checked every published page under `docs/articles/`, `docs/projects/`, `docs/code/`, and both manual overview pages for a frontmatter `description:` field. Found only 2 gaps: `docs/projects/index.md` and `docs/code/index.md` had no frontmatter at all (unlike `articles/index.md` and `manuals/index.md`, which already had full frontmatter blocks) — added `title`, `description`, `date`, `status` to both to match. Every other in-scope page (11 articles, 14 project pages, 3 code sub-pages) already had a description; manual chapters excluded from this pass per the plan (low value, high volume). Descriptions reviewed and approved by site owner before writing. `mkdocs build --strict` clean; spot-checked `<meta name="description">` renders correctly in built HTML for both pages | Direct control over search-result snippets; the frontmatter convention had already been followed everywhere else, so this closed the only 2 real gaps rather than a 30+ file rewrite | SI-003 |
| 2026-07-14 | Added a "Before publishing" checklist to the article template (`C:\GitFolder\00_templates\3_template_blog\docs\articles\_template-article.md` — previously had none) and added two checklist items to both the article and project templates: meta description is a specific 1–2 sentence summary (not a generic tagline), and checked for natural cross-links to/from related existing pages | Bakes the SEO/visibility execution plan's meta-description and internal-linking checks into the publish workflow itself, instead of relying on a one-off retrofit pass or a static CLAUDE.md rule that wouldn't be enforced per-page | SI-003 |
| 2026-07-14 | Internal linking pass (execution plan Item 6) — added 4 cross-links between related pages that didn't reference each other: Prisma Access SASE project page ↔ Prisma Access / SASE training manual (bidirectional — project §3 "Why Prisma Access" points to the manual for underlying SASE/architecture concepts, manual §1 "What Is It?" points to the project as a real-world deployment example); `mkdocs-site-update.md` and `mkdocs-setup.md` articles → GitHub in a Nutshell manual (one-directional — both articles walk through raw `git`/GitHub Pages commands with no explanation, now pointing to Ch18 Push/Fetch/Pull and Ch25 GitHub Pages; skipped the reverse link since the manual's GitHub Pages chapter is a generic reference, not about this specific site). Confirmed NICU AI Strategy Workshop ↔ NICU IRRBB PoC was already bidirectional (done during PRJ-010 publish) — no action needed. Scanned remaining project/code/article pairs for other obvious gaps; found none beyond what's already linked. `mkdocs build --strict` clean | Free, low-risk discovery improvement — connects existing content instead of adding new | — |
| 2026-07-14 | Attempted to enable Material's `social` plugin (auto-generated OpenGraph card images) per execution plan Item 4 — installed `pillow` and `cairosvg` into venv, added both to `requirements.txt`, added `social` to `mkdocs.yml` `plugins:`. `mkdocs build --strict` failed: `cairosvg` needs the native Cairo C library (`libcairo-2.dll`), which has no pip wheel on Windows and isn't present on this machine. Left `pillow`/`CairoSVG` pinned in `requirements.txt` (harmless, ready for when Cairo is available) but removed `social` from `mkdocs.yml` so builds stay clean; deferred as SI-010 | Native dependency missing on Windows, no safe pip-only fix; forcing it through would leave every build emitting crash warnings | SI-010, D-028 |
| 2026-07-14 | Enabled RSS feed via `mkdocs-rss-plugin` — added to `requirements.txt` and `mkdocs.yml`, scoped to `articles/` and `projects/` via `match_path` (manuals excluded as reference material, not "what's new" content), dates sourced from frontmatter `date:`, categories mapped from `tags:`. Discovered along the way: the plugin's default `use_material_social_cards: true` builds per-page card image URLs with Windows-style backslashes (`social\projects\foo.png` instead of `social/projects/foo.png`), producing broken URLs and failing `--strict` builds regardless of whether the `social` plugin is even enabled — set `use_material_social_cards: false` to avoid it. Verified `site/feed_rss_created.xml` and `feed_rss_updated.xml` build with correct titles, descriptions, categories, and links; `mkdocs build --strict` clean (exit 0) | RSS gives readers/aggregators a subscribe path with zero per-post effort | SI-009, D-028 |
| 2026-07-14 | Added `docs/robots.txt` (crawler allow-all + sitemap pointer) and verified it lands at the built site root (`site/robots.txt`), not nested. Also ran a verification-only pass over `site/sitemap.xml`: all 127 URLs use the correct `https://somelamecode.github.io/mkdocs-blog/` base, no localhost/relative paths, no missing pages. Flagged two pre-existing issues without fixing (out of scope for a verification pass): (1) `docs/notebooks/index.md` is a "Coming Soon" placeholder with no frontmatter, indexed in the sitemap like any real page; (2) two orphaned pages — `manuals/github-in-a-nutshell/INDEX.md` and `manuals/prisma-access-sase/INDEX.md` — are leftover source-material READMEs (not in `nav`, near-duplicate of the real overview pages) that still build and land in the sitemap as crawlable duplicate content | Standard crawler hygiene; sitemap correctness needed a direct check rather than assuming `site_url` alone guarantees it | SI-008 |
| 2026-07-14 | Reordered nav entries newest-first in `mkdocs.yml`: `Projects` list, `Code` list, top-level `Manuals` list (Prisma Access / SASE now before GitHub in a Nutshell), and both `Articles` sub-lists (MkDocs & Material, Code & Data); Overview/index/hub entries kept first in each list (not reversed); chapter/part order within both manuals and Lifting Diary's internal sub-pages left untouched — those are reading sequences, not creation-order lists; checked both Articles sub-lists for tutorial dependencies before reversing (none found — each entry is a standalone reference page). Follow-up pass matched the corresponding hub page tables/cards to the new order: `docs/projects/index.md`, `docs/manuals/index.md`, `docs/code/index.md` tables reversed; `docs/articles/index.md` "MkDocs & Material" cluster card refreshed to the newest 5 (also fixed a pre-existing bug where the card had silently dropped "Diagram Examples," the newest article in that cluster, since its publish). Nav-only + content-order change, no files moved or renamed; `mkdocs build` clean | Newest content should be first for a reader browsing "what's new" | — |
| 2026-07-13 | Published NICU IRRBB Proof of Concept project page (PRJ-010) — companion technical/governance deep-dive to PRJ-009: the pipeline architecture (4-step DAG — feature engineering → train 3 XGBoost models → SHAP explainability + 8-quarter backtest in parallel → A/B promotion gate → 6-scenario APRA rate-shock engine → dashboard), the three models replacing static IRRBB assumptions with real gate results (CPR model's gate failure shown honestly, not hidden), a requirement-by-requirement APRA governance mapping, and a tabbed LLM-vs-predictive-ML comparison; no scrub needed — purely technical source material with no client individuals named (D-026); linked GitHub repo checked for names first; 2 original Mermaid diagrams (styled with the explicit-contrast approach from PRJ-009) + 1 cost-comparison chart; cross-linked both ways with PRJ-009 (replaced its "coming in a follow-up post" placeholders); nav entry and projects index row added | PRJ-010 publish | PRJ-010, D-026 |
| 2026-07-13 | Published NICU AI Strategy Workshop project page (PRJ-009) — case study covering a two-day Microsoft Copilot & Agents AI strategy workshop for an Australian credit union: the shadow-AI/unwritten-philosophy tension that prompted it, the evidence case for a strategy engagement over a training session (MIT/IDC/RAND/Gartner/S&P Global failure-rate research), Day 1 (shared vocabulary, honest maturity picture, Purview-first governance sequencing, a live AI Philosophy exercise) and Day 2 (sector context, the automation/generative/predictive decision test applied live, a Crawl/Walk/Run roadmap mapped to the credit union's strategy pillars); light names-only scrub per D-025 (lighter than PRJ-008's full anonymization, per site owner's explicit scope); 2 chart images + 2 original Mermaid diagrams (iterated once for text-contrast — Mermaid's `color:` style doesn't reliably apply to multi-line HTML-labeled nodes, fixed via inline `<span style>` wrapping); IRRBB proof-of-concept technical content deliberately deferred to a planned companion post; nav entry and projects index row added; `status: draft` → `published` after review | PRJ-009 publish | PRJ-009, D-025 |
| 2026-07-10 | Published Prisma Access SASE Implementation project page (PRJ-008) — case study covering a two-legacy-platform-to-Prisma-Access SASE replacement (30 branch sites, 4 data centres, ~5,000 endpoints); fully anonymized from the source engagement (client name, sector/industry, regulatory framework names, country, real domains/IPs/tenant IDs, site codes, and AG/AGD-derived naming all stripped or genericized per D-023) via a three-phase Claude Code workflow (scrub → draft → review-gated nav wiring); 3 Mermaid diagrams + success-criteria pie chart; nav entry and projects index row added; `status: draft` → `published` after review | PRJ-008 publish | PRJ-008, D-023 |
| 2026-07-10 | Added a Manuals card to the homepage's "What's Here" grid (`docs/index.md`) | Manuals had been live since Phase 1 of the rollout but was never surfaced on the homepage — caught while browsing the deployed site locally | D-020 |
| 2026-07-10 | **[Structural, first-time publish]** Published Prisma Access / SASE training manual into `docs/manuals/prisma-access-sase/` — 54 chapters + 4 appendix copied from `C:\GitFolder\02_Projects\07_SASE-PrismaAccess\workspace\` (source project untouched, content complete, no further changes planned there); full nav tree added (9 parts + appendix, same flat pattern as GitHub in a Nutshell); frontmatter normalized to site convention (dropped `track`/`source`/`web_source`/`pdf_delta` authoring metadata, `status: complete` → `published`, tags reformatted to block style); the 9 part-level `README.md` files and appendix `README.md` were not carried over as separate nav pages — their orientation content was folded into a new `docs/manuals/prisma-access-sase.md` overview page instead, keeping the nav shape identical to GitHub in a Nutshell; fixed one broken link in ch54 (`Next: Appendix A` was plain text, not a link); `mkdocs build --strict` clean, images/prev-next/nav spot-checked locally | Manuals restructuring, Phase 4 of 5 — first-time publish is lower-risk than Phase 2's migration since there's no existing URL to preserve | MAN-001, D-020 |
| 2026-07-10 | Addendum to the Manuals migration below: re-enabled `navigation.footer` in `mkdocs.yml` (was fully off, not just CSS-hidden, since the March "slim footer" change) and re-scoped `docs/stylesheets/extra.css` so prev/next chapter links render only on Manuals pages — matched via `html:has(link[rel="canonical"][href*="/manuals/"])` — while staying hidden everywhere else, preserving the original March decision for Articles/Projects/Code | Chapters need real prev/next now that they have a nav tree; verified via headless Chrome screenshots on both a Manuals chapter (shown) and an Articles page (still hidden) | D-020 |
| 2026-07-10 | **[High-risk/structural]** Migrated GitHub in a Nutshell from `docs/projects/github-in-a-nutshell/` into new `docs/manuals/github-in-a-nutshell/` — 29 chapters + 3 appendix + assets moved via `git mv` (history preserved), full nav tree built in `mkdocs.yml` with part-level grouping (7 parts + appendix, each chapter individually listed); `docs/projects/github-in-a-nutshell.md` moved to `docs/manuals/github-in-a-nutshell.md` as the manual's overview page; removed its row from `projects/index.md` and added a pointer to the new Manuals section; old URL `/projects/github-in-a-nutshell/` now 404s (no redirect added, see D-021); `mkdocs build --strict` clean, no broken internal links (chapter cross-references and image paths are all relative within the moved subtree, unaffected by the move) | Manuals restructuring, Phase 2 of 5 — validates the new `Manuals` nav pattern against live content before Prisma Access SASE follows the same shape | D-020, PRJ-002 |
| 2026-07-10 | Built `Manuals` top-level nav section skeleton — hub page (`docs/manuals/index.md`) and `Manuals` nav tab (Overview only, placed between Projects and Code) | Manuals restructuring, Phase 1 of 5 — container only, no content moved yet | D-020 |
| 2026-07-10 | Fixed deprecated `tags_file` option in the `tags` plugin — removed from `mkdocs.yml` and replaced `[TAGS]` marker in `docs/tags.md` with the current `<!-- material/tags -->` directive comment | `mkdocs serve` was emitting a deprecation warning on every start; tag listing still renders correctly with the new syntax | — |
| 2026-05-16 | Published Infrastructure Consolidation project page (PRJ-007) — case study covering 6→3 server consolidation: DC virtualization on Proxmox, SCCM Site Recovery migration, W2016 decommission; 6 key decisions and 6 troubleshooting cases documented; nav entry and projects index row added | PRJ-007 publish | PRJ-007 |
| 2026-05-07 | Added "Testing with Synthetic Audio" section to PRJ-006 (`docs/projects/meeting-audio-notes-pipeline.md`) — covers why synthetic test audio was needed for pipeline development, corporate TTS use cases (IVR/call-centre, e-learning, accessibility, internal comms, voice prototyping), how the tool works (SSML + neural voices), usage command, and link to code page | Surface TTS generator in context of PRJ-006; no new Azure resources required | PRJ-006 |
| 2026-05-07 | Published TTS Audio Generator code page (`docs/code/tts-audio-generator.md`) — setup.sh (Azure Speech resource provisioning) and tts.py (speaker-labelled transcript → MP3 via SSML with gender-matched neural voices) with line numbers; Code index row and nav entry added | Companion code page for PRJ-006 TTS tool | — |

> Rows before 2026-05-07 archived to `_blog/_archive/change-log-2026.md` on 2026-07-14 (50-row threshold).
